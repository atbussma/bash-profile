# 1Password CLI

# Add the 1Password CLI to the PATH
#
#   NOTE: there is an issue capturing or evaluate the 1Password CLI directory variable using this method
#
# export OPPATH=$(find "~/Binaries" -maxdepth 1 -type d -name 'op_*' -print | head -n -1)
export OPPATH=~/Binaries/op_darwin_amd64_v0.5.7
if [ -d $OPPATH ]; then
    export PATH=$PATH:$OPPATH
fi
