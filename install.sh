#!/bin/bash
#
#   .bashrc && .bash files installation for linux and mac os X
#
mkdir -p ~/.bash
cp ./.zshrc ~/.zshrc
cp ./.bashrc ~/.bashrc
cp ./.profile ~/.profile
cp ./.profiles/.bash_profile.* ~/.profiles
